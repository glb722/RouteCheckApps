<%= form_for(@equipment_room) do |f| %>
  <% if @equipment_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipment_room.errors.count, "error") %> prohibited this equipment_room from being saved:</h2>

      <ul>
      <% @equipment_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
